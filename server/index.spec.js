import { expect } from 'chai';

describe('Vrai ou Faux', () => {
  it('true is true', () => {
    expect(true).equal(true);
  });
  it('false is false', () => {
    expect(false).equal(false);
  });
});
